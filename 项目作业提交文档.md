# LLAISYS 项目总结与复现报告

> 作者视角：第一人称  
> 日期：2026-03-16

---

## 1. 运行环境与复现准备

### 1.1 软硬件依赖

| 项目 | NVIDIA 路径 | 沐曦路径 |
|------|------------|---------|
| GPU | NVIDIA RTX 4060 Laptop (8GB) / A100-SXM4-80GB ×8 | MetaX C500 (64GB) |
| 驱动/SDK | CUDA ≥ 12.5, cuBLAS | MACA SDK ≥ 3.0, mcBLAS (`/opt/maca/`) |
| 编译 | xmake ≥ 2.7, GCC ≥ 7.5, nvcc | xmake ≥ 2.7, GCC ≥ 7.5, mxcc |
| Python | ≥ 3.9, torch ≥ 2.4, transformers, safetensors | 同左 (PyTorch metax 版) |
| 分布式 | NCCL (GPU) / OpenMPI (CPU) | mxCCL (规划中) |
| OS | Ubuntu 20.04+ / WSL2 | Ubuntu 20.04+ |

### 1.2 编译、安装与运行

> **前置条件**：确保 NVIDIA GPU 可用（`nvidia-smi` 正常显示设备）且 `torch.cuda.is_available()` 返回 `True`。

#### Step 0：下载模型权重（必须先完成）

```bash
# 方式 1：Python 自动下载（推荐）
python3 -c "from huggingface_hub import snapshot_download; snapshot_download('deepseek-ai/DeepSeek-R1-Distill-Qwen-1.5B', local_dir='./models/DeepSeek-R1-Distill-Qwen-1.5B')"
# 约 3.3 GB，需要几分钟。HuggingFace 被墙时加环境变量：
# export HF_ENDPOINT=https://hf-mirror.com

# 方式 2：huggingface-cli
# huggingface-cli download deepseek-ai/DeepSeek-R1-Distill-Qwen-1.5B \
#     --local-dir ./models/DeepSeek-R1-Distill-Qwen-1.5B
```

下载完成后目录应包含 `config.json`、`tokenizer.json`、`model.safetensors` 等文件。

#### Step 1：编译与安装

```bash
# ===== NVIDIA GPU 路径 =====
xmake f -m release --nv-gpu=y && xmake -j$(nproc) && xmake install
pip install ./python/

# ===== 沐曦 C500 路径 =====
xmake f -c --metax-gpu=true && xmake -j$(nproc) && xmake install
pip install ./python/

# ===== NVIDIA + 分布式 (NCCL + MPI) =====
xmake f -m release --nv-gpu=y --dist-nccl=y --dist-mpi=y && xmake -j8 && xmake install
pip install ./python/
```

#### Step 2：验证推理

```bash
# NVIDIA GPU 推理测试
# 注意：test_infer.py 会先运行 HuggingFace 推理再运行 llaisys 推理，
#       --device nvidia 要求 GPU 可用、模型权重已下载到 --model 指定目录
# 注意：8GB 显存 GPU 上，maxseq 默认值 32768 会导致 VRAM 溢出触发换页，
#       测试脚本已默认限制 max_seq_len=4096 以避免此问题
python3 test/test_infer.py --model ./models/DeepSeek-R1-Distill-Qwen-1.5B --test --device nvidia

# CPU 推理测试（无 GPU 时可用，但速度慢）
python3 test/test_infer.py --model ./models/DeepSeek-R1-Distill-Qwen-1.5B --test --device cpu

# 沐曦三层验证 (Runtime → 算子 → 端到端推理)
python3 test/test_metax.py --model ./models/DeepSeek-R1-Distill-Qwen-1.5B
```

#### Step 3：量化（可选）

```bash
python3 scripts/quantize.py --model ./models/DeepSeek-R1-Distill-Qwen-1.5B --output ./quantized_model --bits 8
python3 scripts/quantize.py --model ./models/DeepSeek-R1-Distill-Qwen-1.5B --output ./quantized_model_int4 --bits 4 --group-size 128
```

#### Step 4：启动推理服务

```bash
cd python && python3 -m server.app --model ../quantized_model_int4 --device nvidia --port 8000
# 沐曦: 将 --device nvidia 替换为 --device metax
```

#### Step 5：TP 分布式服务（32B AWQ, 多卡）

```bash
LLAISYS_AWQ_NATIVE=0 python3 scripts/tp_server.py \
    --tp-size 2 \
    --model /home/alopbrobro8848/llaisys-cuda-engine/models/qwen2.5-32b-instruct-awq \
    --device nvidia --gpus 5,6 --port 8002
```

- **参数含义**：
  - **`LLAISYS_AWQ_NATIVE=0`**：AWQ INT4 权重先转换/缓存为 FP16（推理更快；首次加载会更慢）
  - **`--tp-size 2`**：张量并行（TP）切分为 2 个 rank
  - **`--model .../qwen2.5-32b-instruct-awq`**：本地 32B AWQ 模型目录（需包含权重与 `config.json`）
  - **`--device nvidia`**：使用 NVIDIA CUDA 后端
  - **`--gpus 5,6`**：绑定可见 GPU 列表（脚本内会设置 `CUDA_VISIBLE_DEVICES`）
  - **`--port 8002`**：对外提供 Web/API 的服务端口（rank 0）

---

## 2. 核心解题思路

### 2.1 算子优化：CUDA 端 Dequantize 与 Linear 的重写

**Linear 算子**：我将矩阵乘法完全委托给 cuBLAS (`cublasGemmEx`)，支持 FP32/FP16/BF16 三种精度，Decode 阶段 M=1 退化为 GEMV，cuBLAS 内部自动选最优路径。Bias 加法由单独的 `add_bias_kernel` 完成。针对 FP16 权重 + FP32 输入的混合精度场景，我使用线程局部 FP16 缓冲区进行输入转换，再走 cuBLAS F16×F16→F32 GEMM，避免每次推理分配/释放显存。

**Dequantize 算子**：实现了三条反量化路径——

1. **INT8 per-channel**：`W_fp32 = W_int8 × scale`，每行一个 scale，计算简单高效。
2. **INT4 per-group (g=128)**：从 `uint8` 解包 2 个 INT4，按 group 查 scale 并反量化到 FP32。第一轮优化将粒度从"1 输出/线程"改为"1 packed byte/线程（写 2 个输出）"，减少重复读取。
3. **AWQ Native INT4 CUDA Kernel**：直接从 AWQ 原始 `qweight(int32)/qzeros/scales` 格式实时反量化。核心难点是 AWQ GEMM 的**交错位打包顺序** `[0,16,4,20,8,24,12,28]`（非顺序 `[0,4,8,...,28]`），调试时通过穷举 shift 顺序并与 transformers 参考 logits 对比定位根因。

所有反量化结果被送入同一 `linear` 算子，C++ 侧 `linear_maybe_dequant` 按 `dtype` 自动分流，对外推理 API 无需任何修改。

**六项 GPU 推理优化（118s → 2.73s，43× 加速）**：

| # | 优化 | 做法 | 效果 |
|---|------|------|------|
| 1 | cuBLAS Handle 全局复用 | `static thread_local cublasHandle_t` 惰性初始化，整个生命周期只创建一次 | `cudaDeviceSynchronize` 81K→0 次 |
| 2 | Grow-Only 显存缓冲区 | self_attention 的 5 个临时 buffer 只增长不释放，`ensure_buf()` 复用 | `cudaMalloc` 73K→740 次 |
| 3 | Batched Self-Attention | 消除逐 head 循环，gather 所有 head 到 `[nh, seq, dh]`，用 `cublasSgemmStridedBatched` 一次完成 | kernel launch 370K→164K 次 |
| 4 | Residual 指针交换 | `std::swap(residual, hidden_states)` O(1) 操作替代 D2D memcpy | 消除 4928 次 D2D 拷贝 |
| 5 | 异步 memcpy | H2D/D2D 改为 `memcpy_async`，每 token 仅 1 个同步点（D2H 读 argmax） | 消除同步屏障 |
| 6 | PyTorch 显存池释放 | `torch.cuda.empty_cache()` 释放 VRAM，消除 WSL2 环境下换页开销 | 57s→2.73s |

### 2.2 异构设备集成：Context/Runtime 机制与沐曦适配

我采用三层设备抽象设计：

```
Context（线程级） → Runtime（设备级） → RuntimeAPI（函数指针表，12 个统一接口）
```

每个线程持有一个 `Context`，管理该线程使用的所有 `Runtime` 实例。`Runtime` 通过 `LlaisysRuntimeAPI` 函数指针表屏蔽底层差异，包含 `malloc/free/memcpy/stream` 等 12 个接口。算子层 `src/ops/<op>/op.cpp` 按 `deviceType()` 路由到对应后端实现。新增设备只需：

1. `include/llaisys.h` 新增枚举值 `LLAISYS_DEVICE_METAX = 2`
2. `src/device/metax/` 实现 RuntimeAPI 的 12 个函数
3. `src/ops/<op>/metax/` 移植每个算子 kernel
4. `xmake/metax.lua` 配置编译规则
5. 各 `op.cpp` 增加 `#ifdef ENABLE_METAX_API` 分发分支

**沐曦 C500 适配要点**：

- MACA SDK 路径 `/opt/maca/`，API 使用 `mc*` 前缀（`mcMalloc`/`mcFree`/`mcMemcpy`），BLAS 库为 `mcblas`，BF16 类型为 `__maca_bfloat16`。
- MXMACA kernel 语法与 CUDA 高度兼容（`__global__`/`blockIdx`/`threadIdx` 一致），主要差异为编译器 (`mxcc`) 和头文件。
- 构建系统使用 `on_build` 自定义编译 `.mc` 文件（后缀改为 `.mc` 避免 xmake CUDA 规则冲突），编译参数：`mxcc --cuda-gpu-arch=xcore1000 -x maca -fPIC -std=c++17 -O2`。
- 链接使用 `--whole-archive` 解决静态库符号丢弃问题。

**跑通指令**：

```bash
# 沐曦平台一键部署
chmod +x scripts/deploy_metax.sh && ./scripts/deploy_metax.sh ./quantized_model_int4

# 或手动
xmake f -c --metax-gpu=true && xmake -j$(nproc) && xmake install && pip install ./python/
python3 test/test_metax.py --model ./models/DeepSeek-R1-Distill-Qwen-1.5B  # 17/17 PASS
python3 -m server.app --model ../quantized_model --device metax --port 8000
```

### 2.3 KV-Cache 与批处理

**KV-Cache 基础机制**：每层维护 K/V 各一块预分配缓存 `[maxseq, nkvh, dh]`。写入时按 `current_pos` 追加当前 token 的 K/V，读取时 `slice(0, 0, pos+1)` 零拷贝取历史窗口。

**TrieNode 前缀树 + Snapshotting**：我实现了 C++ 侧的前缀树 KV-Cache 池。`SaveCache` 将设备端 KV-Cache 拷贝到 CPU snapshot，`RestoreCache` 回写设备。新请求 prefill 前调用 `cache_pool_lookup(input_ids)` 查找最长前缀匹配，命中则从 snapshot 恢复，跳过已计算部分。请求完成时 `cache_pool_insert(all_tokens, snapshot)` 保存到池中。这在多轮对话和会话切换场景下显著减少重复计算。

**连续批处理 (Continuous Batching)**：`InferenceEngine` 后台 Worker 线程循环执行四阶段：

1. **Admit**：从请求队列取新请求 → 分配空闲 Slot → prefill（含前缀匹配）
2. **Decode**：对所有活跃 Slot 执行一步 `batch_decode`（embedding/linear/rope/rms_norm/swiglu 均批量化，self_attention 因各 Slot KV 长度不同逐 Slot 执行）
3. **Finish**：完成请求释放 Slot，保存 KV-Cache 到前缀树池
4. **Idle Wait**：无请求时休眠 50ms

C++ 侧 `LlaisysQwen2BatchContext` 预分配 `max_batch_size` 个独立 Slot，每个 Slot 拥有完整的逐层 KV-Cache 和独立 `current_pos` 追踪。批量 GEMM 由 `cublasSgemmStridedBatched` 完成。实测 4 路并发非流式请求加速比约 **4.00×**。

### 2.4 分布式推理与 32B 模型适配

**通信抽象**：我设计了 `DistComm` 接口，仅暴露 TP 前向所需的最小原语：`all_reduce_sum`/`all_gather`/`broadcast`/`barrier`。三个后端实现：

- `MockComm`：no-op，用于开发验证
- `NcclComm`：GPU 端 NCCL 真实通信，unique ID 通过文件共享（`LLAISYS_NCCL_ID_FILE`），rank 0 生成后其他 rank 轮询读取
- `MpiComm`：CPU 端 OpenMPI，`MPI_Init_thread(MPI_THREAD_MULTIPLE)` 初始化，GPU 数据自动经 CPU 缓冲区中转

**张量并行 (TP) 实现**：

- **权重分片**：按参数名自动分类——Q/K/V/gate/up 走 Column Parallel（按输出维切分），O/down 走 Row Parallel（按输入维切分），embedding/norm 不切分。`classifyWeight()` + `slice2D()`/`slice1D()` 在 CPU 侧完成。
- **前向链路**：O proj 和 down proj 后插入 `allReduceIfTP()`，调用 NCCL `ncclAllReduce(in-place, sum)`。
- **KV-Cache TP 化**：cache shape 改为本地 head 视图 `[maxseq, local_nkvh, dh]`，`SaveCache`/`RestoreCache` 自动记录 `tp_size/tp_rank` 元信息并校验跨 TP 配置恢复。

**TP Server 架构**：为解决"NCCL `allReduce` 要求所有 rank 同时调用，但只有 rank 0 接收用户请求"的问题，我实现了 TPCoordinator 架构——rank 0 作为 Web Server + Coordinator，通过 TCP 协议广播 prefill/decode 命令到 follower ranks，所有 rank 同步执行后 NCCL allReduce 汇合。

**32B 模型适配 (Qwen2.5-32B-Instruct-AWQ)**：

- 模型架构参数从 `config.json` 自动读取（64 层、hidden=5120、40 heads、8 kv_heads），不再硬编码。
- AWQ 转换性能优化：将 Python for 循环逐组解量化改为全向量化实现（torch broadcast + reshape），448 次转换从 600s 降至 145s（**提速 ~15×**）。
- FP16 转换缓存：首次加载后缓存至 `.llaisys_cache/`，二次加载 ~10s。
- Native AWQ GPU Kernel：CUDA INT4→FP32 反量化 kernel + AWQ 交错位打包修正，无需 CPU 侧转换。
- 在 8×A100 上跑通 tp=1/2/4/8 四种配置，**所有 TP 配置输出比特级精确一致**。

**显存需求估算（32B AWQ，经验值）**：

- **TP=2**：每卡约 **35–45 GB**，总计约 **70–90 GB**
- **TP=4**：权重按 1/4 切分但 KV-Cache 基本不变，常见仍为每卡约 **25–35 GB**（总计约 **100 GB 级别**）

**跑通指令**：

```bash
# 编译 (NCCL + MPI)
xmake f -m release --nv-gpu=y --dist-nccl=y --dist-mpi=y && xmake -j8 && xmake install
pip install ./python/

# 端到端 TP 推理测试 (32B AWQ, tp=4)
python3 test/test_tp_e2e.py --model /path/to/Qwen2.5-32B-Instruct-AWQ --tp-size 4

# TP 服务 (2卡, 32B AWQ)
LLAISYS_AWQ_NATIVE=0 python3 scripts/tp_server.py \
    --tp-size 2 \
    --model /home/alopbrobro8848/llaisys-cuda-engine/models/qwen2.5-32b-instruct-awq \
    --device nvidia --gpus 5,6 --port 8002

# NCCL 通信测试 (2/4/8 GPU)
python3 test/test_nccl_real.py

# MPI 通信测试 (2/4/8 进程)
python3 test/test_mpi_real.py
```

---

## 3. 性能对比与测试结果

### 3.1 GPU 推理优化（DeepSeek-R1-Distill-Qwen-1.5B, RTX 4060, 88 tokens）

| 阶段 | 总耗时 | 吞吐量 (tok/s) | 单 token 延迟 | 加速比 |
|------|--------|---------------|-------------|--------|
| 优化前（基线） | 118.0s | 0.75 | ~1341ms | 1× |
| +Handle 复用+Grow-Only | 59.0s | 1.49 | ~670ms | 2.0× |
| +Batched Attention+指针交换 | 57.4s | 1.53 | ~652ms | 2.1× |
| **+异步 memcpy+显存池释放（最终）** | **2.73s** | **27.0** | **~30ms** | **43.2×** |
| HuggingFace Transformers 参考 | 2.89s | 26.2 | ~33ms | — |

### 3.2 沐曦 C500 推理性能（DeepSeek-R1-Distill-Qwen-1.5B）

| 模型格式 | 权重大小 | 压缩比 | 吞吐量 (tok/s) | 测试通过 |
|---------|---------|--------|---------------|---------|
| FP32 原始 | 3.3 GB | 1.0× | **25.6** | 17/17 ✅ |
| INT8 量化 | 2.4 GB | 1.4× | **16.4** | 17/17 ✅ |
| INT4 量化 | 0.8 GB | 3.8× | **18.6** | 17/17 ✅ |

### 3.3 分布式 TP 推理（Qwen2.5-32B-Instruct-AWQ, 8×A100-80GB, 10 tokens）

| TP 配置 | 加载时间 | 推理时间 | 加速比 | 输出正确性 |
|---------|---------|---------|--------|----------|
| tp=1 | 142.2s | 1.51s | 1.00× (基线) | ✅ 基线 |
| tp=2 | 184.0s | 1.32s | **1.14×** | ✅ 与 tp=1 比特级一致 |
| tp=4 | 275.3s | 1.35s | **1.12×** | ✅ 与 tp=1 比特级一致 |
| tp=8 | 486.0s | 15.26s | 0.10× | ✅ 与 tp=1 比特级一致 |

> tp=2/4 在短序列已有加速；tp=8 通信开销在 10 token 短序列下显著，长序列场景预期大幅改善。

### 3.4 多用户并发批处理

| 场景 | 结果 |
|------|------|
| 单请求非流式 | PASS |
| 4 路并发非流式 | PASS，加速比 **4.00×** |
| 3 路并发流式 (SSE) | PASS |
| 引擎状态查询 | PASS |

### 3.5 测试汇总

| 测试类别 | 通过数 |
|---------|--------|
| 框架层 (Mock + 边界) | 21/21 |
| NCCL 2/4-GPU 通信 | 24/24 |
| MPI 2/4/8 进程通信 | 27/27 |
| C++ smoke tests | 4/4 |
| 端到端 TP 推理 (32B AWQ, tp=1/2/4/8) | 4/4 |
| 沐曦 C500 全量验证 (Runtime+算子+推理) | 17/17 |
| **总计** | **80/80 + 17/17** |

### 3.6 Profiling 瓶颈消除总结

通过 nsys 系统级 profiling，我将 GPU 推理的 Host 端瓶颈从占比 **95%**（cudaDeviceSynchronize 71.9s + cudaMalloc/Free 40.4s）压降到接近零——每 token 的 CPU-GPU 同步点从 **~920 次降至 1 次**（仅保留 D2H 读 argmax 结果），最终实现与 HuggingFace 持平甚至略优的推理性能。
